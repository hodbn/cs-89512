# Exercise 1 - Game of Gossip

The simulator is written in Svelte, and builds to a static website.

## Building

You must have `yarn` installed.

```bash
yarn run build
```

## Development mode

You must have `yarn` installed.

```bash
yarn install

yarn run dev
```
